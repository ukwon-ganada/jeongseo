# 네이버웍스 메시지방 ↔ 「사건정리」 시트 매칭 설정 가이드

「사건정리」 구글시트에 **「방-사건 매칭」 탭**을 추가합니다.

- 🔴 **메시지방만 있음** — 네이버웍스에 방은 있는데 시트에 사건이 없는 것
- 🟠 **시트에만 있음** — 시트에는 사건이 있는데 메시지방이 없는 것
- ⚠️ **확인 필요** — 방 하나에 사건이 여러 건 걸리는 것(동명이인 등)
- 🟢 **정상 매칭** — 방과 사건이 1:1로 맞는 것

시트를 고치거나 방이 생기고 없어질 때마다 **자동으로 다시 계산**됩니다.

| 파일 | 내용 |
|---|---|
| `appsscript/nw_match.gs` | 시트에 붙이는 Apps Script 모듈 전체 |

---

## 먼저 알아둘 것 — 네이버웍스에는 "방 목록 전체 조회" API가 없습니다

네이버웍스 봇 API에는 **봇이 속한 대화방 ID를 한꺼번에 받아오는 기능이 없습니다.**
(공식 개발자 포럼 답변: *"모든 메시지방의 ID를 일괄적으로 불러올 수 있는 기능은 없는 걸로 알고있습니다."*)

그래서 방 목록은 **「메시지방 목록」 탭**에 쌓아두고 그걸 기준으로 대조합니다.
이 탭을 채우는 방법이 두 가지이고, **둘 다 써도 됩니다.**

| 방법 | 준비 | 갱신 시점 |
|---|---|---|
| **A. 손으로 붙여넣기** | 없음 (바로 가능) | 붙여넣는 즉시 |
| **B. 봇 연동** | 네이버웍스 봇 1개 + 각 방에 봇 초대 | 방이 생기거나 없어지는 즉시(자동) |

**A만 해도 매칭 탭은 완전히 동작합니다.** B는 "방 목록을 사람이 안 챙겨도 되게" 만드는 단계입니다.

---

## 1단계 — 스크립트 붙이기 (필수 · 약 5분)

1. 「사건정리」 시트를 엽니다.
2. 상단 메뉴 **확장 프로그램 → Apps Script**.
3. 왼쪽 **파일** 옆 **＋ → 스크립트**를 눌러 새 파일을 만들고 이름을 `nw_match` 로 합니다.
   - ⚠️ 기존 파일(기일가져오기·변경기록 등)에 덮어쓰지 마세요. **새 파일**로 추가합니다.
4. 이 저장소의 **`appsscript/nw_match.gs` 내용을 전부 복사**해 붙여넣고 **저장(💾)**.
5. 편집기 위쪽 함수 선택창에서 **`NWM_installTriggers`** 를 고르고 **실행(▶)**.
   - 처음 실행하면 권한 승인 창이 뜹니다 → 계정 선택 → **고급 → (프로젝트 이름)(으)로 이동 → 허용**.
6. 시트로 돌아오면
   - **「방-사건 매칭」** 탭과 **「메시지방 목록」** 탭이 생기고,
   - 상단에 **「🔗 방-사건 매칭」 메뉴**가 생깁니다.

> **기존 스크립트와 충돌하지 않습니다.** 이 모듈의 함수 이름은 전부 `NWM_` 로 시작하고,
> `onOpen`·`onChange` 도 이름을 겹치지 않는 **설치형 트리거**로 붙입니다.
> 단 하나, **`doPost` 만 예외**입니다 — 3단계 참고.

---

## 2단계 — 방 목록 채우기

### 방법 A — 손으로 붙여넣기 (지금 바로)

**「메시지방 목록」** 탭 **A열**에 네이버웍스 대화방 이름을 한 줄에 하나씩 적습니다.
(네이버웍스 PC 앱의 대화방 목록에서 이름을 그대로 옮겨 적으면 됩니다.)

| A 방 이름 | B 방 ID | C 상태 | D 최근 확인 | E 출처 | F 메모 |
|---|---|---|---|---|---|
| 천준명 특가법(향정) | | | | | |
| 2026고합204 천옥희 | | | | | |
| 정서 사무국 공지방 | | **제외** | | | 사건방 아님 |

- **C열에 `제외`** 라고 적으면 그 방은 대조에서 빠집니다. 공지방·내부방을 여기에 넣으세요.
- **C열에 `나감`** 은 봇이 나간 방을 표시할 때 자동으로 들어갑니다(직접 적어도 됩니다).
- 붙여넣고 10초쯤 지나면 「방-사건 매칭」 탭이 알아서 다시 계산됩니다.

### 방법 B — 봇이 자동으로 채우게 (3단계)

---

## 3단계 — 네이버웍스 봇 연동 (선택 · 자동화하고 싶을 때)

### 3-1. 네이버웍스 Developer Console 설정

<https://developers.worksmobile.com> → 로그인 → **Console**

1. **App 추가** → 앱 이름 아무거나(예: `사건방 매칭`).
2. **API 2.0 → App 정보**에서 **Client ID**, **Client Secret** 확인.
3. **Service Account** 발급 → **Service Account ID**(`xxxx.serviceaccount@도메인` 형태)와
   **Private Key(.pem 또는 .key 파일)** 를 내려받습니다. *(키는 이때만 받을 수 있습니다.)*
4. **OAuth Scopes** 에 **`bot`**, **`bot.read`** 를 추가합니다.
5. **Bot 등록** → 봇 이름(예: `사건방 도우미`) → 등록 후 **Bot ID**(숫자) 확인.
   - **"봇을 대화방에 초대 허용"** 옵션을 켭니다.
6. 관리자 화면(Admin)에서 그 봇을 **사용 설정**합니다.

### 3-2. 스크립트 속성에 값 넣기

Apps Script 편집기 → 왼쪽 **⚙️ 프로젝트 설정** → 아래 **스크립트 속성 → 속성 추가**

| 속성 이름 | 값 |
|---|---|
| `NW_CLIENT_ID` | Developer Console 의 Client ID |
| `NW_CLIENT_SECRET` | Client Secret |
| `NW_SERVICE_ACCOUNT` | Service Account ID (`....serviceaccount@도메인`) |
| `NW_PRIVATE_KEY` | Private Key **파일 내용 전체** (`-----BEGIN PRIVATE KEY-----` 줄부터 끝까지) |
| `NW_BOT_ID` | Bot ID (숫자) |
| `NW_CALLBACK_KEY` | 아무 긴 문자열 하나 (예: `jsq7f2m9x4` — 아래 콜백 URL 에 씁니다) |

> 키는 시트가 아니라 **스크립트 속성**에만 넣습니다. 시트를 공유해도 키는 노출되지 않습니다.
> 여러 줄이 한 칸에 안 들어가면 줄바꿈을 `\n` 두 글자로 바꿔 한 줄로 붙여넣어도 됩니다(스크립트가 알아서 복원합니다).

넣은 뒤 시트에서 **「🔗 방-사건 매칭」 → ⚙️ 연동 상태 확인** 을 눌러 6개가 모두 ✅ 인지 봅니다.

### 3-3. 웹 앱으로 배포하고 Callback URL 등록

방이 생기거나 없어지는 걸 **즉시** 반영하려면 봇 콜백을 연결합니다.

1. Apps Script 편집기 → 오른쪽 위 **배포 → 새 배포 → 유형 선택 → 웹 앱**
   - **실행 계정**: 나
   - **액세스 권한**: **모든 사용자**  ← 네이버웍스 서버가 호출해야 하므로 필수
2. 배포 후 나오는 **웹 앱 URL** 을 복사합니다.
3. **이 프로젝트에 `doPost` 함수가 이미 있는지 확인합니다.**
   - 없으면 → `nw_match` 파일 맨 아래에 다음 3줄을 추가하고 다시 배포합니다.
     ```javascript
     function doPost(e) {
       return NWM_doPost(e);
     }
     ```
   - 이미 있으면 → 기존 `doPost` 안 마지막에 `return NWM_doPost(e);` 한 줄을 넣습니다.
4. Developer Console → **Bot → Callback URL** 에 아래 형식으로 등록합니다.
   ```
   (웹 앱 URL)?k=NW_CALLBACK_KEY에_넣은_값
   ```
5. **Callback 이벤트**에서 **`join`(봇 초대됨)**, **`leave`(봇 내보내짐)**, **`message`** 를 켭니다.

### 3-4. 각 사건 방에 봇 초대

네이버웍스에서 **사건 대화방마다 그 봇을 초대**합니다.
초대되는 순간 봇이 방 이름을 읽어 「메시지방 목록」에 자동으로 추가하고,
방에서 내보내면 상태가 `나감`으로 바뀝니다.

> **왜 초대가 필요한가요?** 네이버웍스 API는 "봇이 들어가 있는 방"만 조회할 수 있습니다.
> 봇이 없는 방은 외부에서 이름조차 알 수 없어, 그 방은 A방법(손으로 적기)으로만 넣을 수 있습니다.

---

## 자동 갱신이 도는 방식

| 무엇이 바뀌면 | 어떻게 감지하나 | 반영까지 |
|---|---|---|
| 시트의 사건이 추가·수정·삭제됨 | `onChange` 설치형 트리거 | 약 10초 |
| 「메시지방 목록」을 손으로 고침 | `onChange` 설치형 트리거 | 약 10초 |
| 방에 봇이 초대됨 / 내보내짐 | 봇 Callback(`doPost`) | 즉시 |
| 방 이름이 바뀜 | 10분 주기 트리거가 API로 다시 확인 | 10분 이내 |
| 그 밖의 모든 누락 | 10분 주기 트리거 | 10분 이내 |

- 연속으로 여러 칸을 고쳐도 **10초에 한 번**만 다시 계산합니다(구글 실행 한도 절약).
- 스크립트가 매칭 탭에 쓰는 동안의 변경은 무시하므로 **무한 루프가 생기지 않습니다.**
- 급할 때는 **「🔗 방-사건 매칭」 → 🔄 지금 새로고침**.

---

## 매칭 규칙

시트의 사건 탭(「변경기록」 제외)을 전부 훑어서, 각 행의 **성명**과 **사건번호**를 뽑습니다.
그리고 **방 이름 안에 그 값이 들어있는지**로 맞춥니다.

1. **사건번호가 들어있으면 사건번호로 맞춥니다** (가장 정확). `2026고합204`·`26고합204` 둘 다 인식합니다.
2. 사건번호가 없으면 **성명**으로 맞춥니다.
3. `강경아,정영식` 처럼 여러 명이 적힌 칸은 **각 이름으로 따로** 맞춥니다.
   `이현우(이현우1)` 의 괄호 안, `김대진 外2` 의 `外2`, 주민번호도 알아서 걸러냅니다.

### 방 이름 짓는 요령

현재 시트에는 **같은 이름의 사건이 28쌍** 있습니다(예: 천옥희 3건, 양민경 5건, 김태현 3건).
이런 사건의 방 이름이 성명뿐이면 ⚠️ **확인 필요**로 빠집니다.

👉 **방 이름에 사건번호를 같이 넣으면** 동명이인도 정확히 갈립니다.

```
좋음:  2026고합204 천옥희 사기
좋음:  천옥희(2026고단1277)
아쉬움: 천옥희          ← 어느 사건인지 알 수 없어 ⚠️ 로 감
```

### 종결 사건

**종결** 체크가 켜진 행과 **「종결사건」 탭**의 행은 방이 없어도 정상이므로
🟠 목록에서 빼고, 대신 요약줄에 *"종결된 사건 N건은 제외했습니다"* 로만 표시합니다.

---

## 문제 해결

**「방-사건 매칭」 탭이 안 생겨요**
→ Apps Script 편집기에서 `NWM_installTriggers` 를 다시 실행하고 권한 승인을 마쳤는지 확인하세요.

**갱신이 안 돼요**
→ 「🔗 방-사건 매칭」 → **⚙️ 연동 상태 확인** 에서 *자동 갱신 트리거: 켜짐* 인지 봅니다.
꺼져 있으면 **⏱ 자동 갱신 켜기**.

**토큰 발급 실패(401)**
→ `NW_PRIVATE_KEY` 가 `-----BEGIN PRIVATE KEY-----` 부터 끝까지 통째로 들어갔는지,
`NW_SERVICE_ACCOUNT` 가 사람 계정이 아니라 **서비스 계정 ID** 인지 확인하세요.
Developer Console 의 **OAuth Scopes 에 `bot`, `bot.read`** 가 있어야 합니다.

**방 조회 실패(403/404)**
→ 그 방에서 봇이 이미 나갔거나 방이 삭제된 경우입니다. 상태가 `나감`으로 바뀌고 대조에서 빠집니다.

**공지방·내부방이 자꾸 🔴 에 떠요**
→ 「메시지방 목록」 탭 그 줄의 **C열에 `제외`** 라고 적으세요.

**되돌리고 싶어요**
→ 「🔗 방-사건 매칭」 → **⏹ 자동 갱신 끄기** 를 누르고, 두 탭을 지우면 원래대로입니다.
이 모듈은 **기존 탭의 값을 절대 고치지 않습니다**(읽기만 합니다).
